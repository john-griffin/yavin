<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><a href="#">Crawls</a></h1>
    </li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <li class="">
        {{#if session.isAuthenticated}}
          <a {{ action 'invalidateSession' }}>Logout</a>
        {{else}}
          {{#link-to 'login'}}Login{{/link-to}}
        {{/if}}
      </li>
    </ul>
  </section>
</nav>

{{outlet}}
